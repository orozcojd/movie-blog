import state from '@/store/errors/state'
import getters from '@/store/errors/getters'
import actions from '@/store/errors/actions'
import mutations from '@/store/errors/mutations'

export default {
	namespaced: true,
	state,
	getters,
	actions,
	mutations
}